export const BUTTON = Object.freeze({
  default: 'button',
  ACTIVE: 'button_active',
});

export const ICON = Object.freeze({
  default: 'icon',
  SIZE: Object.freeze({
    SM: 'icon_size_sm',
    LG: 'icon_size_lg',
  }),
});
